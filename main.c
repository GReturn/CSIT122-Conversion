#include "Conversion.h"
#include "myTools.h"

int main() {
	menu();
    
    // int hours, mins, sec;
	// int time;
	// int coin;
	// int q, d, p;
	
	// int lineX = 0;
    // int lineY = 0;
    // char input;
    // char wait;
    // do {
    //     system("cls");
    //     drawSBox(lineX,lineY,lineX+25,lineY+15);
    //     input = userInput();
    //     processInput(input, &time, &coin, &hours, &mins, &sec, &q,&d,&p);
    //     scanf(" %c", &wait);
    // } while(input != 'd');
	return 0;
}